// Fixture: reference names hidden in identifiers and paths must still be found (regression: the
// lint matched whole words only). Four findings: lines 3 to 6.
struct KestrelHull {};
static int cinder_reach_seed = 0;
#define TALLIS_ORBIT_KM 400
const char* kPath = "records/saltmarch_outpost.jsonc";
