#pragma once
// Log channel shared by the authority module's sources.
#include "helios/core/log.h"

namespace helios::authority {
HELIOS_LOG_CHANNEL(LogAuthority, "Authority");
} // namespace helios::authority
